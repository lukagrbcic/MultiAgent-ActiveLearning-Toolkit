import numpy as np


class multi_agent_system:

    def __init__(self, initial_prompt, n_agents, epochs, baseline_score, elite_archive_size=4,
                    agent_iterations=20,
                    exploit_patientce=3,
                    explore_interval=10,
                    agent_model='lbl/cborg-coder:latest',
                    agent_idea_model='lbl/cborg-coder:latest',
                    evaluator='local')




run_llm = llm_discovery(initial_query, 100, 0.630, model) # 10, 10, 100 setup
result = run_llm.loop()
               hard_reseed_interval=10, exploit_patience=3):
